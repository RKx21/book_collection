<h1>Welecome to the Library</h1>

<a href="/tasks/new">Add a book</a> <br/>
<%= link_to('Show a Book', {controller: 'tasks', action: 'index', page: 1}) %><br />
<%= link_to("Edit first book", edit_task_path(@task)) %> <br />
<%= link_to("Delete first book", delete_task_path(@task)) %> <br />


<h2> Books in Library</h2>
<ul>
<% @books.reverse_each do |book| %>
  <li><%= book.title %></li>
<% end %>
</ul>
